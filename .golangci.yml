version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gocritic
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
