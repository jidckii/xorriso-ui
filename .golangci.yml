version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gocritic

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
